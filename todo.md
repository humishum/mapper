
Collect more data. 

fix logging for models 

# Reconstructions: 
Run VGGT(and also long variant) and da3-streaming, compare outputs
look into VGGT specific bundle adjustment 
how do we get global pose/orientation? 
Add frame filtering, for frames that end up being trash -> needs to be codeveloped with filtering out respective telemetry

align different videos, look at mission peak data for experimentation on loop closure

# Viewer: 
integrate 3d map tiles as base layer for prototype viewer
better design 

LOD loading
