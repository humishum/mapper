# VGGT Experiment Configuration
#
# Run with:
#   python -m src.experiments.runner --config configs/experiments/vggt_test.yaml

name: "vggt_trail_test"

model: "vggt"

model_config:
  model_name: "facebook/VGGT-1B"
  weights_path: null  # Optional: local model.pt if you downloaded manually
  use_chunking: true
  window_size: 10
  window_overlap: 2
  device: "cuda"
  image_size: 518
  preprocess_mode: "crop"  # square | pad | crop
  use_point_map: false
  min_confidence: 1.0
  max_points: 50000
  sample_seed: 0

input_folder: "/workspace/data/videos"
output_folder: "/workspace/output/vggt_gopro"

fps: 10
align_to_gps: true
force_reprocess: false
save_window_results: true
frame_cache_dir: null  # Optional: override cache root (defaults to output_folder/_frame_cache)

alignment_config:
  window_alignment_method: "pose_overlap"
  min_overlap_frames: 1
  allow_scale: "auto"
  pose_overlap_max_rmse: 2.0
  icp_voxel_size: 0.1
  icp_max_corr_dist: 0.2
  icp_init: "none"
  gps_scale_min_std_dev_m: 0.5
  save_alignment_debug: true

# use for just gopro stuff

video_extensions:
  - ".MP4"
